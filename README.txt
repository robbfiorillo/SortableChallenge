INSTRUCTIONS:

Replacing the listing.txt and products.txt with other data files is sufficient, 
however, there are constants defined within the index.php which can be edited 
to chose alternate data files.

To Run execute php on index.php

outputs:
results.txt 
	-comprised of all unique listing.title matches to a single product name

duplicateResults.txt 
	-comprised of all listing.title which have matched multiple products